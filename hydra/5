voronoi(200)
// osc(10)
// .colorama(() => Math.sin(time * 0.4))
// .pixelate(40)
// .scrollX(time)
// .shift(5, 5, 0, 1)
.kaleid(20)
.rotate(() => time * 0.2)
.modulate(noise(() => Math.sin(time * 0.4) + 1))
// .mask(noise(1000).modulate(osc()))
.repeat(1,2)
.mask(noise(100).modulate(osc(10)))
.mult(gradient(0.2))
// .colorama(() => Math.random())
.out()
